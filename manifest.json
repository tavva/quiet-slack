{
	"name": "Quiet Slack",
	"version": "1.0",
	"description": "Use Slack without knowing if there are any new messages",
	"manifest_version": 2,

	"background": {
		"scripts": ["scripts/background.js"]
	}
}
